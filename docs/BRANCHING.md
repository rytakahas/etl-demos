# Branching

Recommended:
- Keep your working implementation in `feat/graphdb`
- Create a deployment/runbook branch `feat/azure-fabric-prod` for Azure/Fabric docs + local compose + infra skeleton

Then open a PR from `feat/azure-fabric-prod` into `feat/graphdb` (or main), depending on your release flow.
