name: dbt CI

on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  dbt-ci:
    runs-on: ubuntu-latest

    env:
      DBT_PROJECT_DIR: dbt
      DBT_PROFILES_DIR: dbt
      GOOGLE_APPLICATION_CREDENTIALS: gcp-dbt-sa.json

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dbt-bigquery
        run: |
          python -m pip install --upgrade pip
          pip install "dbt-bigquery==1.10.3"

      - name: Sanity check dbt project files
        run: |
          test -f "${DBT_PROJECT_DIR}/dbt_project.yml"
          test -f "${DBT_PROFILES_DIR}/profiles.yml"
          echo "OK: dbt_project.yml and profiles.yml found."

      - name: Decide CI mode (build vs compile-only)
        shell: bash
        run: |
          if [ -z "${{ secrets.GCP_SERVICE_ACCOUNT_KEY }}" ]; then
            echo "SKIP_BQ=true" >> "$GITHUB_ENV"
            echo "No GCP secret available. Will run compile-only."
          else
            echo "SKIP_BQ=false" >> "$GITHUB_ENV"
            echo "GCP secret available. Will run full build."
          fi

      - name: Write GCP service account key
        if: env.SKIP_BQ == 'false'
        shell: bash
        run: |
          printf '%s' "${{ secrets.GCP_SERVICE_ACCOUNT_KEY }}" > "${GOOGLE_APPLICATION_CREDENTIALS}"

      - name: dbt deps
        run: |
          dbt deps --project-dir "${DBT_PROJECT_DIR}" --profiles-dir "${DBT_PROFILES_DIR}"

      - name: dbt compile (no warehouse)
        if: env.SKIP_BQ == 'true'
        run: |
          dbt compile --project-dir "${DBT_PROJECT_DIR}" --profiles-dir "${DBT_PROFILES_DIR}" --target dev

      - name: dbt build (ci)
        if: env.SKIP_BQ == 'false'
        run: |
          dbt build --project-dir "${DBT_PROJECT_DIR}" --profiles-dir "${DBT_PROFILES_DIR}" --target ci

